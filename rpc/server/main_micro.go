package main

import (
	"fmt"
	"win-his/server"
)
/* *************************************这是实现micro微服务用例*************************************
****************************************micro默认自动分配端口，默认支持consul**********************************
**********************************************************************************************/
func main() {
	if err := server.Run(); err != nil {
		fmt.Println(err)
	}
}
